{
  "name": "yt-playlist-pruner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Batch-select and remove videos from your YouTube playlists (unpacked MV3 Chrome extension).",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.25.0",
    "tsx": "^4.20.0",
    "typescript": "^5.7.0"
  }
}
